Implement the forward pass `swish_forward(x)` that mimics memory-efficient Swish.

Requirements:
- Return `x * torch.sigmoid(x)`.
- Just implement the basic function (no autograd.Function needed here, just the math).