Implement `focal_loss(logits, targets, alpha=0.25, gamma=2.0)` for binary classification.

Requirements:
- `logits`: [B] unnormalized scores.
- `targets`: [B] binary labels (0 or 1).
- Compute the focal loss and return the mean.