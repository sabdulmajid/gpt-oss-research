Implement `prepare_components(accelerator, model, optimizer, dataloader)`.

Requirements:
- Use the `accelerator.prepare` method to prepare the given components.
- Return the prepared model, optimizer, and dataloader.