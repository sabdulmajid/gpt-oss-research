Implement `shift_sequence(x, shift)`.

Requirements:
- `x` has shape `[batch, seq_len, dim]`.
- Roll the `seq_len` dimension by `shift` steps to the right (positive shift means right).
- Use PyTorch operations.