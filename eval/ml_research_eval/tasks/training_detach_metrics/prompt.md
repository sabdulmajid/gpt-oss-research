Implement `detach_metrics_tree(tree)`.

Requirements:

- recursively walk dictionaries, lists, and tuples
- convert scalar tensors to Python scalars
- convert non-scalar tensors to nested Python lists
- leave non-tensor leaves unchanged

