Implement `batch_matrix_trace(matrices)` in PyTorch.

Requirements:
- `matrices` has shape `[batch, N, N]`
- compute the trace (sum of diagonal elements) for each matrix in the batch
- return a tensor of shape `[batch]`