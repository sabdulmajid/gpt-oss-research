# Data Conventions

`data/manifests/` stores frozen JSON manifests generated from the YAML hypotheses in `configs/datasets/`.

`data/samples/` stores tiny local fixtures for smoke validation only. These files are not research evidence and must never be confused with a real training corpus.

